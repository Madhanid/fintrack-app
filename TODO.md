# TODO: Run Project\n\n✅ Create TODO.md (done)\n✅ Edit backend/Dockerfile to add runtime dependencies (done)\n✅ docker compose down (done)\n⏳ docker compose build --no-cache backend (running)\n- [ ] docker compose up -d\n- [ ] Verify docker compose ps (containers Up)\n- [ ] Test localhost:3000 and :8080\n- [ ] Complete task

